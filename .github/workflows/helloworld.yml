name: "Hello World"

on: 
    push:
        branches:
            -master
            -production

jobs: 

    hello_world:

        runs-on: ubuntu-latest
        steps: 
            - name: "Print hey"
              run: echo " Hello abhi babe"
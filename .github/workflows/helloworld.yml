name: "name upper is hello world Workflow"

on:
  push:
     branches:
        - master 
        - production
        
jobs:
    hello_world:
        runs-on: ubuntu-latest
        steps: 
            - name: "second name is Print hellow world"
              run: echo "running third repository"